node_modules/
dist/
.env
*.local
.DS_Store
android/build/
android/.gradle/
android/app/build/
android/app/release/
*.keystore
*.jks
pnpm-lock.yaml
UPDATEMAY5.zip

# Cohere AI spend log — machine-local, never commit
COHERE_SPEND_LOG.json

# Governance audit run artifacts — gitignored; only .gitkeep tracked
runs/governance/*.json
